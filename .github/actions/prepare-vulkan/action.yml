name: "Prepare Vulkan SDK (Ubuntu)"
description: >
  Install the Vulkan SDK + runtime for a given Ubuntu runner variant.
  Variants mirror the inline steps previously duplicated in build.yml
  (l6-vulkan-composite-action): "24.04" (x64), "24.04-arm64", "22.04".

inputs:
  ubuntu-version:
    description: >
      Ubuntu variant: "24.04" (x64), "24.04-arm64" (aarch64), or "22.04" (x64).
    required: true

runs:
  using: "composite"
  steps:
    - name: Prepare Vulkan SDK for Ubuntu 24.04 (x64)
      if: inputs.ubuntu-version == '24.04'
      shell: bash
      run: |
        wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-1.3.290-noble.list https://packages.lunarg.com/vulkan/1.3.290/lunarg-vulkan-1.3.290-noble.list
        sudo apt update
        sudo apt install vulkan-sdk -y
        sudo apt-get install -y mesa-vulkan-drivers

    - name: Prepare Vulkan SDK for Ubuntu ARM64
      if: inputs.ubuntu-version == '24.04-arm64'
      uses: jakoch/install-vulkan-sdk-action@v1
      with:
        vulkan_version: 1.4.335.0
        cache: true

    - name: Install Vulkan runtime libraries (Ubuntu ARM64)
      if: inputs.ubuntu-version == '24.04-arm64'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y libvulkan-dev mesa-vulkan-drivers

    - name: Prepare Vulkan SDK for Ubuntu 22.04 (x64)
      if: inputs.ubuntu-version == '22.04'
      shell: bash
      run: |
        wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-1.3.290-jammy.list https://packages.lunarg.com/vulkan/1.3.290/lunarg-vulkan-1.3.290-jammy.list
        sudo apt update
        sudo apt install vulkan-sdk -y
        sudo apt-get install -y mesa-vulkan-drivers
